import { FC } from 'react';

const RemoveContact:FC = () => {
  return (
    <button className='bg-red-500 hover:bg-red-700 text-white py-2 px-4 rounded'>Remove All</button>  
    );
};

export default RemoveContact